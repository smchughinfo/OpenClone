@page
@model LoginModel

@{
    ViewData["Title"] = "Sign in to OpenClone";
}

<div class="row justify-content-center min-vh-100 align-items-top mt-5">
    <div class="col-12 col-sm-8 col-md-6 col-lg-4">
        <div class="card border-0 shadow-sm">
            <div class="card-body p-5">
                <div class="text-center mb-4">
                    <h2 class="h4 mb-2">Sign in to OpenClone</h2>
                    <p class="text-muted small">Welcome back! Continue with your Google account</p>
                </div>

                @{
                    if ((Model.ExternalLogins?.Count ?? 0) == 0)
                    {
                        <div class="alert alert-warning text-center">
                            <i class="bi bi-exclamation-triangle me-2"></i>
                            Google authentication is not configured. Please contact support.
                        </div>
                    }
                    else
                    {
                        <form id="external-account" asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                            @foreach (var provider in Model.ExternalLogins!)
                            {
                                <div class="d-grid mb-3">
                                    <button type="submit" class="btn btn-outline-dark btn-lg py-3" name="provider" value="@provider.Name" title="Sign in using your @provider.DisplayName account">
                                        <svg class="me-2" width="18" height="18" viewBox="0 0 24 24">
                                            <path fill="#4285f4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                                            <path fill="#34a853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                                            <path fill="#fbbc05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                                            <path fill="#ea4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                                        </svg>
                                        Sign in with @provider.DisplayName
                                    </button>
                                </div>
                            }
                        </form>
                        
                        <div class="text-center mt-4">
                            <small class="text-muted">
                                Don't have an account? 
                                <a href="/Identity/Account/Register?returnUrl=@Model.ReturnUrl" class="text-decoration-none">Sign up with Google</a>
                            </small>
                        </div>
                    }
                }
            </div>
        </div>
        
        <div class="text-center mt-3">
            <small class="text-muted">
                By continuing, you agree to our 
                <a href="/Terms" class="text-decoration-none">Terms of Service</a> and 
                <a href="/Privacy" class="text-decoration-none">Privacy Policy</a>
            </small>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
